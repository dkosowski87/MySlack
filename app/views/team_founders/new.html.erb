<%= title("Sign up for #{@team.name}") %>

<%= standard_form [@team, @team_founder], url: team_team_founder_path do |f| %>
	<%= render partial: 'users/form', locals: {f: f} %>
	<%= f.submit 'Sign up' %>
<% end %>